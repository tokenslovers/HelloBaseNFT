// package.json
{
  "name": "HelloBaseNFT",
  "version": "1.0.0",
  "description": "A simple dApp for minting Hello World NFTs on the Base blockchain.",
  "main": "index.js",
  "scripts": {
    "start": "cd frontend && npm start",
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network base"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@openzeppelin/contracts": "^4.7.0",
    "ethers": "^5.7.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "hardhat": "^2.10.0"
  }
}
